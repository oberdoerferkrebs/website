// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// `site` will be set to the real domain once we go live (used for SEO / sitemaps).
export default defineConfig({
  site: 'https://oberdoerferkrebs.example',
});
